name: Build & Deploy to Oracle Kubernetes

on:
  push:
    branches:
      - main

jobs:
  deploy:
    # 👇 THIS is the key line you asked for
    runs-on: [self-hosted, oracle, arm]

    steps:
      # 1️⃣ Pull code onto the Oracle VM
      - name: Checkout source code
        uses: actions/checkout@v4

      # 2️⃣ Build Docker image locally on Oracle VM (ARM)
      - name: Build Docker image
        run: |
          docker build -t test-app-ci-cd:latest .

      # 3️⃣ (Optional) Show cluster connection
      - name: Verify Kubernetes access
        run: |
          kubectl get nodes

      # 4️⃣ Deploy to Kubernetes
      - name: Deploy application
        run: |
          kubectl apply -f k8s/
